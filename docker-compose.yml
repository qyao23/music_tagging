services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: music-tagging-backend
    ports:
      - 8000:8000
    volumes:
      - ./backend:/app
    restart: always

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: music-tagging-frontend
    ports:
      - 8080:80
    volumes:
      - ./frontend:/app
    depends_on:
      - backend
    restart: always